import styled from "styled-components";

const ChatRoom = () => {
  return (
    <div>
      <ChatInput>
        <input />
        <button>보내기</button>
      </ChatInput>
    </div>
  );
};

const ChatInput = styled.div``;

export default ChatRoom;
